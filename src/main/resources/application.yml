spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/leyka_bot
    username: root
    password: 'idiNax*2'
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  config:
    import:
      - classpath:config.yml
      - classpath:localization.yml